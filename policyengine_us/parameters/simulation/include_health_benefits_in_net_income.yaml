description: |
  If true, net-income variables return household_net_income_including_health_benefits
  (which already adds healthcare_benefit_value).  Default is false for
  backward-compatibility.
values:
  0000-01-01: false
metadata:
  datatype: bool
