description: Utah applies the dependent exemption reform, if this is true (modifies only the per-dependent exemption inside the Taxpayer Tax Credit, leaving the shared personal-exemption amount for other uses untouched).

values:
  0000-01-01: false

metadata:
  unit: bool
  period: year
  label: Utah dependent exemption reform in effect
