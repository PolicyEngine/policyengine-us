description: Massachusetts provides this dependent exemption amount per eligible dependent under the contributed reform. A negative sentinel uses the baseline per-dependent amount (a no-op at default); a non-negative value applies a flat per-dependent amount.

values:
  0000-01-01: -1

metadata:
  unit: currency-USD
  period: year
  label: Massachusetts dependent exemption reform amount
