description: Hawaii applies the dependent exemption reform, if this is true (modifies only dependent exemptions, not personal exemptions).

values:
  0000-01-01: false

metadata:
  unit: bool
  period: year
  label: Hawaii dependent exemption reform in effect
