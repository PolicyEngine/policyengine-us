YAML tests mirroring policyengine-household-api customer fixtures (Amplifi, Impactica, MyFriendBen) under tests/policy/baseline/partners/, plus a dedicated CI runner so partner-affecting changes are visible in PRs. Also adds analytics_coverage/ — 177 per-signature test cases (grouped by state) covering every variable combination partners have requested in production.
