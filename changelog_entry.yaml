- bump: patch
  changes:
    fixed:
      - Fixed coverage collection in GitHub Actions by running tests directly with coverage instead of through subprocess scripts
      - Split baseline tests into separate workflow steps (states vs federal) to manage memory usage
      - Added coverage summary display in GitHub Actions and PR comments
