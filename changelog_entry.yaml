- bump: patch
  changes:
    fixed:
      - Fixed GitHub Actions workflows by using system Python installation with `uv pip install -e ".[dev]" --system`
      - Aligned PR and push workflows to use identical test commands for consistency
      - Updated both workflows to use Python 3.13
      - Added pytest>=8.3.0 to dev dependencies to ensure test runner is available
