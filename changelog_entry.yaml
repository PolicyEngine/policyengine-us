- bump: patch
  changes:
    changed:
      - Use `default` arg in `select` statements.
      - Add `defined_for` to variables.
      - Rename .py files to match variable name.
      - Move taxcalc variable aliasing to yaml mapping file.
